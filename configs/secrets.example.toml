# Copy this file to secrets.toml and fill in the actual values.
# secrets.toml must NOT be committed to version control.

[database]
# Required if using PostgreSQL
# password = "password"

[jwt]
# Paths to RSA key files for JWT signing/verification
private_key_path = "certs/private.pem"
public_key_path = "certs/public.pem"
